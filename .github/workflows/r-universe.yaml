name: Test R-universe

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: R-universe testing
    uses: r-universe-org/workflows/.github/workflows/build.yml@v3
    with:
      # FIXME: revert to 'bioc' after spatialdataR has been accepted to BioC
      universe: biocstaging
      organization: bioconductor
